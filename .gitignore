# dependencies
/node_modules
/.pnp
.pnp.js

# runtime data (prototype disk store + uploads)
/data
/public/uploads

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — ignore ALL (they hold service-role keys / SESSION_SECRET)
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# never commit markdown (docs/reports/handoffs stay local)
*.md
.vercel
